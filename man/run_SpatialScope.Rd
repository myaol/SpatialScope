% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_SpatialScope.R, R/run_app.R
\name{run_SpatialScope}
\alias{run_SpatialScope}
\title{Launch SpatialScope Shiny App}
\usage{
run_SpatialScope(data_path = NULL, ...)

run_SpatialScope(data_path = NULL, ...)
}
\arguments{
\item{data_path}{Optional path to a user data file (.rds, .csv, etc.) or "demo" to load example data.}

\item{...}{Additional arguments passed to \code{shiny::runApp()}.}

\item{seurat_input}{Seurat object, path to .rds file, or "demo" for example data}

\item{sample_name}{Optional dataset name}

\item{show_image}{Logical; whether to display H&E image}
}
\description{
Wrapper for run_spatial_selector() so users can call
run_SpatialScope("demo") or use their own Seurat object.

Launch the Shiny interface for SpatialScope. Users can either:
\itemize{
\item Call \code{SpatialScope::run_SpatialScope()} to open the app and upload their own data interactively, or
\item Call \code{SpatialScope::run_SpatialScope("path/to/data.rds")} or \code{SpatialScope::run_SpatialScope("demo")} to launch the app preloaded with a dataset.
}
}
\examples{
\dontrun{
SpatialScope::run_SpatialScope()
SpatialScope::run_SpatialScope("demo")
SpatialScope::run_SpatialScope("~/Downloads/mydata.rds")
}

}
